spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ulsan_project
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
